/**
 * Flip to `true` in GitHub to show the Region filter in the map Filters dialog.
 * Uses the Media Locations → Region column in Airtable (already loaded in the app).
 */
export const ENABLE_REGION_FILTER = false;


/**
 * "on"  = auto-open a random entry when the map loads.
 * "off" = show the entry list and full catalog map view.
 */
export const RANDOM_ENTRY_ON_LOAD = "off";
